import styles from './PreviewPane.module.css';

// Replaced with the sandboxed iframe Preview in a later phase.
export function PreviewPane() {
  return (
    <div className={styles.pane}>
      <p className={styles.placeholder}>Preview lands in a later phase.</p>
    </div>
  );
}
